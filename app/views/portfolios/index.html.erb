<div class="album text-muted">
	<div class="container">
		<%= paginate @portfolio_items %>

		<div id="sortables">
			
			<% @portfolio_items.each do |portfolio_item| %>
				<%= render 'portfolio_item', portfolio_item: portfolio_item %>
			<% end %>

		</div>
		<%= paginate @portfolio_items %>

	</div>
</div>